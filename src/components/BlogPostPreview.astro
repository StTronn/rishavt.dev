---
export interface Props {
  post: any;
}

const { post } = Astro.props;
---

<article class="post-preview">
  <!-- <p class="publish-date">{post.frontmatter.publishDate}</p> -->
  <a href={post.frontmatter.url}><u><h5 class="title">{post.frontmatter.title}</h5></u></a>
  <!-- <small class="publish-date">{post.frontmatter.publishDate}</small> -->
  <!-- <small class="publish-date">{post.frontmatter.publishDate}</small> -->
  <!-- <p class="publish-date">{post.frontmatter.publishDate}</p> -->
  <p>{post.frontmatter.description}</p>
  <small class="publish-date">{post.frontmatter.publishDate}</small>
  <!-- <a href={post.frontmatter.url}>Read more</a> -->
</article>

<style>
  .content :global(main > * + *) {
    margin-top: 1rem;
  }

  .post-preview {
    padding-bottom: 2rem;
    margin-bottom: 2rem;
    border-bottom: 4px solid var(--theme-divider);
  }

  .post-preview-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  header {
    display: flex;
    flex-direction: column;
    text-align: left;
    align-items: flex-start;
    justify-content: center;

    padding-bottom: 1rem;
  }

  .author,
  .publish-date {
    margin: 0;
    padding:0;
  }

  .publish-date,
  .author {
    color: var(--theme-text-lighter);
  }

</style>
